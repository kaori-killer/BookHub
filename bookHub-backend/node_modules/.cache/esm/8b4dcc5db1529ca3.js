let validationResult;_b15‍.x([["validate",()=>validate]]);_b15‍.w("express-validator",[["validationResult",["validationResult"],function(v){validationResult=v}]]);

       const validate = (req, res, next) => {
  const errors = validationResult(req);
  if (errors.isEmpty()) {
    return next();
  }
  return res.status(400).json({ message: errors.array()[0].msg });
};
